import React from 'react'
import ReactDOM from 'react-dom/client'
import AppAIForward from './AppAIForward.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AppAIForward />
  </React.StrictMode>,
)
